let a = "Prachi"
let b = "Raunak"
let c = "Sourav"

//______We are exporting the module by default, we can import by using another name (as for a we use name)________
export default a;

//If we just export then we have to import that variable with same name
export {b,c};